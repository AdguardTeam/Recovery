language: node_js
node_js:
  - "7"
install:
  - npm install
script:
  - npm start
  - npm run ghtest
  - npm test
  - npm run zip
deploy:
  - provider: pages
    skip_cleanup: true
    local_dir: 'dist'
    github_token: $GITHUB_TOKEN_A
    on:
      branch: master
